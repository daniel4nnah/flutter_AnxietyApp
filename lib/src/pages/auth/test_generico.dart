/* import 'package:flutter/matterial';

class TestGeneric extends StatelessWidget {
  const TestGeneric({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
} */